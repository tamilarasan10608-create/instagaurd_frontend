{
  "name": "instaguard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "powershell -ExecutionPolicy Bypass -File ./restart.ps1"
  }
}
